# =============================================================================
# DESACTIVAT (juliol 2026): el pipeline és 100% GitHub Actions + GitHub Pages.
# Es conserva per si cal reactivar ingestió local / servidor propi.
# =============================================================================
#
# services:
#   ingest:
#     build:
#       context: .
#       dockerfile: docker/Dockerfile
#     container_name: on-son-les-bicis-ingest
#     restart: unless-stopped
#     env_file:
#       - .env
#     environment:
#       DB_PATH: /data/history.db
#     volumes:
#       - ./db:/data
#       - .:/app
#       - ./deploy/ssh:/root/.ssh:ro
